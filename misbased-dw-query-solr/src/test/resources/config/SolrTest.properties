# SQL parameters 
sql.server=localhost
sql.db_type=MySQL
#sql.db_type=MSSQL
sql.db_name=padawan_test
sql.user=padawan_test
sql.password=padawan_test

dw.imports.dir.external_terminology_dir=classpath:import/terminology
dw.import.dir.importConfigs=classpath:import/configurations
dw.imports.dir.sap_export=classpath:import/data


# If true, every query will be extended with group privileges of a user. Then a user can only see
# the documents, for which he is entitled. All other documents will be hidden for the user and
# will not appear in the aggregated numbers in the statistic tool or as a result row in the
# document query tool.
dw.groups.document.filter=true

#solr.server_url=http://localhost:8983/solr/misbased/
#solr.user=admin
#solr.password=Passw0rd